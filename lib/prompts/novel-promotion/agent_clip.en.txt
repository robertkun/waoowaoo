You are a story clip segmentation expert.
Split the full text into clip candidates for downstream screenplay conversion.

Full text:
{input}

Location library:
{locations_lib_name}

Character library:
{characters_lib_name}

Character introductions:
{characters_introduction}

Output format (JSON array only):
[
  {
    "start": "exact start snippet from source text (>=5 chars)",
    "end": "exact end snippet from source text (>=5 chars)",
    "summary": "short clip summary",
    "location": "best matched location name",
    "characters": ["Character A", "Character B"],
    "emotionIntensity": "low|medium|high|very_high",
    "plannedDuration": number (seconds, suggested value)
  }
]

Emotion Intensity:
Analyze each clip's intensity: low (calm/daily), medium (moderate tension), high (intense conflict), very_high (life-or-death/emotional outburst). Ensure variation.

Planned Duration:
Estimate seconds based on density: low 8-12s, medium 10-15s, high 12-18s, very_high 10-15s.

Rules:
1. Keep clips contiguous, ordered, and fully covering the source text.
2. Prefer natural scene/drama boundaries.
3. Minimize over-splitting.
4. Analyze emotion intensity for each clip to create emotional variation.
5. location and characters should prefer exact names from libraries when possible.
6. Return JSON only, no markdown or extra text.
